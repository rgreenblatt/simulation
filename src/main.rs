fn main() {
  // let mesh = Rc::new(RefCell::new(Mesh::new(
  //   Vec::new(),
  //   Vec::new(),
  //   None,
  //   None,
  //   true,
  // )));
  // let set = mesh.clone();
  // let mut scene = window.add_mesh(set, Vector3::new(1.0, 1.0, 1.0));
  // scene.set_color(1.0, 0.0, 0.0);
  // scene.enable_backface_culling(false);
  // mesh.replace(Mesh::new(
  //   base_vertices
  //     .iter()
  //     .map(|v| Point3::from(v.coords + addr))
  //     .collect(),
  //   indices.clone(),
  //   None,
  //   None,
  //   true,
  // ));

  println!("Hello, world!");
}
