use crate::LoadedMesh;
use nalgebra::{Matrix3, Vector3};

pub struct SimMesh {
  // might not be needed...
  // node_positions_obj_space: Vec<Vector3>, // per node
  // inv_barycentric_mat: Vec<Matrix3>,      // per tet
  // tetras: Vec<[u16; 4]>,                  // per tet
  // boundary_faces: Vec<[u16; 3]>,
  // node_neighbors_start_end: Vec<[usize; 2]>,
  // neighbors: Vec<usize>,
}

// impl Sim
